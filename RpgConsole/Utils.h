#pragma once
#include <iostream>
#include <chrono>
#include <thread>
#include <string>

using namespace std;

void DisplayTime(string chaine);
void GetAscii();
void CenterRow(int size);
void CenterCol(int size);